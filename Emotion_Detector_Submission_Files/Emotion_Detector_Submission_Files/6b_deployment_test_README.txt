6b_deployment_test.png

This file is intentionally a note rather than a fabricated screenshot.
Run `python3 server.py`, open http://localhost:5000/, and capture the actual
Skills Network deployment interface. Save the screenshot as 6b_deployment_test.png.
